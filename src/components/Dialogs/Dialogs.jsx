import React from "react";
import styles from "./Dialogs.module.css"

const Dialogs = (props) => {
    return (
        <div className={`${styles.red} ${styles.content}`}>
            Dialogs
        </div>
    )
}

export default Dialogs;
